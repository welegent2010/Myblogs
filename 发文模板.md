---
title: "把这里改成你的文章标题"
date: 2026-04-02
lastmod: 2026-04-02
draft: true

description: "用一句话概括这篇文章写什么，建议 20~40 字。"
summary: "这里填写列表页摘要，建议 40~90 字，尽量直接说明核心观点。"

categories:
  - 工作记录

tags:
  - 城市规划
  - 设计研究

series:
  - 工作笔记

showTableOfContents: true
showComments: true
---

> 这是一份发文模板。
> 使用方法：复制整篇，新建一个 `.md` 文件，粘贴后只改需要填写的内容。
> 注意：日期一定要写成 `YYYY-MM-DD`，例如 `2026-04-02`。

# 把这里改成正文大标题

<!--more-->

## 一、文章导语

在这里写开头导语。

## 二、核心内容

在这里继续写正文。

## 三、结论或总结

在这里写收尾。

---

## 填写说明

- `title`：文章标题，必须保留双引号
- `date`：发布日期，格式必须是 `YYYY-MM-DD`
- `lastmod`：最后修改日期，通常和 `date` 一样
- `draft`：正式发布写 `false`；模板本身保留 `true`
- `description`：SEO 简介，一句话说明主题
- `summary`：首页和列表页摘要
- `categories`：分类，可写多个
- `tags`：标签，可写多个
- `series`：系列，可选，不需要就删掉整个字段块

## 发布前自检

- [ ] 标题已填写
- [ ] 日期格式是 `YYYY-MM-DD`
- [ ] `description` 已填写
- [ ] `summary` 已填写
- [ ] `draft` 已改成 `false`
- [ ] 正文标题和内容已填写